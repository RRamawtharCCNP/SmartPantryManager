<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="true" android:label="Smart Pantry Manager" android:theme="@style/Theme.SmartPantry">
        <activity android:name=".ActivityReportActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".RecipeDetailActivity" />
        <activity android:name=".SuggestedRecipesActivity" />
        <activity android:name=".IngredientFormActivity" />
        <activity android:name=".PantryActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
